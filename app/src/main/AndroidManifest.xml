<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.care_pet_my"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ui.me.ChangeUserInfoActivity"></activity>
        <activity android:name=".ui.me.UserInfoActivity" />
        <activity android:name=".ui.me.setting.SetSecurityActivity" />
        <activity android:name=".ui.login.FindPwdActivity" />
        <activity android:name=".ui.me.setting.ModifyPswActivity" />
        <activity android:name=".ui.me.setting.SettingActivity" />
        <activity android:name=".ui.login.LoginActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/demo" />

        <activity android:name=".ui.login.RegisterActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>